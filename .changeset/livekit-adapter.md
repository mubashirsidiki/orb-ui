---
'orb-ui': minor
---

Add `createLiveKitAdapter` for LiveKit Agents. The adapter supports managed rooms, externally managed rooms, LiveKit agent state attributes, attached remote agent audio, and normalized output volume for orb-ui themes.
